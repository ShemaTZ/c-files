#include <stdio.h>
#include <math.h>

int main (void)
{
    double A = sqrt(81);
    double B = pow(3,9);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.99);
    double F = fabs(-100);
    double G = log(10);
    double H = sin(90);
    double I = cos(90);
    double J = tan(90);

    printf("%lf",G);
}